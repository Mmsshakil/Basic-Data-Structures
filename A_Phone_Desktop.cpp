#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        int x, y;
        cin >> x >> y;
        int square = x * 4;
        int total = square + y;

        if (square)
        {
            /* code */
        }
        
    }

    return 0;
}